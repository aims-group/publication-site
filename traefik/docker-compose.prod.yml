version: "3"

volumes:
  production_traefik: {}
  production_traefik_log: {}

services:
  traefik:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        host_name: ${DOMAIN_NAME}
        backend_prefix: ${DOMAIN_SUBDIRECTORY}
    image: pubhub_production_traefik
    volumes:
      - production_traefik:/etc/traefik/ssl
      - production_traefik_log:/var/log/traefik
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:443"
