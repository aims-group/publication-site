version: "3"

volumes:
  production_traefik: {}

services:
  traefik:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        host_name: ${DOMAIN_NAME}
        backend_prefix: ${DOMAIN_SUBDIRECTORY}
        ssl_cert_file: ${SSL_CERT_FILE}
        ssl_cert_key: ${SSL_CERT_KEY}
    image: pubhub_production_traefik
    volumes:
      - production_traefik:/etc/traefik/ssl
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:443"
